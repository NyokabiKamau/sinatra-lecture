source "https://rubygems.org"

gem 'sinatra', '~> 3.0', '>= 3.0.5'

gem 'activerecord', '~> 7.0', '>= 7.0.4.2'

gem 'rake', '~> 13.0', '>= 13.0.6'

gem 'sqlite3', '~> 1.6', '>= 1.6.1'

gem 'sinatra-activerecord', '~> 2.0', '>= 2.0.26'

# Puma is a simple, fast, threaded, and highly parallel HTTP 1.1 server for Ruby/Rack applications. 
gem 'puma', '~> 6.1'

# require all allows you to import all files
gem 'require_all', '~> 3.0'

# used when you only want to use a gem once e.g during development and not deployment
group :development do 
    gem 'rerun', '~> 0.14.0'
end
